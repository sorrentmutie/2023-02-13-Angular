import { FormatProductNamePipe } from './format-product-name.pipe';

describe('FormatProductNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatProductNamePipe();
    expect(pipe).toBeTruthy();
  });
});
